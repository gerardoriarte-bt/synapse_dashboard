/** B · Composición de dashboards · Fase 4 (F4.6–F4.16).
 *
 *  Sin construir. Valida contra `/config/blocks` usando `catalog/blocks.ts`, que
 *  ya está escrito.
 */
export function Builder() {
  return <main className="p-6 text-dim">Builder · F4.6</main>
}
